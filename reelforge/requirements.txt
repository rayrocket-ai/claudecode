# ReelForge Python dependencies.
#
# Kept deliberately separate from the repo's root requirements.txt -- ReelForge
# shares no code with the realtor bot and must not perturb its dependency set.

# Transcription: word-level timestamps are the foundation of every cut.
faster-whisper==1.2.1
ctranslate2>=4.5,<5

# Analysis: face detection for auto-reframe focus points, frame sampling.
opencv-python-headless>=4.10,<5
numpy>=1.26,<3

# Emoji rasterisation. Emoji are composited as PNGs rather than drawn by
# libass, whose colour-glyph support fails silently on headless Linux.
Pillow>=10.0,<12

# EDL validation -- the schema is a contract, so it is enforced, not assumed.
jsonschema>=4.23,<5

# Inbox watcher (server mode).
watchdog>=5.0,<7

# Link fetching. Pinned loosely and updated often on purpose -- yt-dlp breaks
# whenever a site changes, and a stale version fails on exactly the links you
# care about.
yt-dlp>=2025.1.1

# Config.
tomli>=2.0; python_version < "3.11"
