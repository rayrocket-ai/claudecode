# Core
python-telegram-bot[job-queue]>=21.0
anthropic>=0.39.0
pydantic-settings>=2.1.0

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# PDF generation (fallback)
reportlab>=4.0.0

# Browser automation (TransactionDesk / REALM)
playwright>=1.44.0

# Logging
structlog>=23.2.0

# HTTP
httpx>=0.25.0

# Utilities
python-dateutil>=2.8.0
# num2words removed (docopt build issue) — using inline implementation

# Testing (dev)
pytest>=8.0.0
